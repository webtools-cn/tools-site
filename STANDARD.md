# Free ToolBase 全站质量标准 v1.0（2026-08-11）

> 本文件是 opencode 对 free-toolbase.com 全站进行测试、修复、内容升级的唯一执行依据。
> 所有修改必须满足以下标准，否则视为不合格。

---

## 一、任务定义

对 `/home/chison/tools-site`（7388 页，CN 3693 + EN 3695）进行系统性质量测试与修复：
1. **JS 语法修复**：当前 141 页 JS 语法失败（清单见 `/tmp/js_fails_0811.json`），必须全部修复
2. **全站质量检测**：按下方标准逐项扫描，发现问题立即修复
3. **内容升级**：对单薄工具页（<800字符）补充差异化内容，对功能单薄工具（≤3输入+≤2按钮+无canvas）做功能增强
4. **修复后必须验证**：每修一批跑全站扫描确认数字下降，浏览器实测确认功能可用

---

## 二、质量红线（触碰任何一条=不合格）

1. **JS 语法错误**：node new Function 编译失败
2. **功能不可用**：点按钮报错/无输出（浏览器实测）
3. **浅色背景**：页面背景不是深色主题（#0f172a 系）
4. **假评分**：aggregateRating 假数据
5. **EN 页面大量中文**（lang-switch 的"中文"按钮除外）
6. **CN 页面 body 大量英文**
7. **结构失衡**：div/section/main/footer 开闭不匹配
8. **内容段插在 footer 内**（结构性损坏）
9. **空白 shell 工具**：只有 input+button 无说明文字

---

## 三、深色主题标准（强制）

| 元素 | 值 |
|:-----|:---|
| 页面背景 | `--bg: #0f172a` |
| 卡片背景 | `--card-bg: #1e293b` |
| 主文字 | `#e2e8f0` 或 `#f1f5f9` |
| 次要文字 | `#94a3b8` |
| 强调色 | `#06b6d4` 系 |
| 边框 | `rgba(148,163,184,.1)` |

**绝对禁止：**
- ❌ `#fff`/`#ffffff`/`#f8f9fa` 做背景
- ❌ `#333`/`#666` 做文字色
- ❌ 任何浅色主题
- ❌ `--surface:#ffffff`（输入框/textarea 白底+白字不可见）
- ❌ `var(--bg,#f0f0f0)` 浅色 fallback
- ❌ `color:#374151`（深灰在深底上不可见）
- ❌ `:root` 引用 `var(--surface)`/`var(--text)` 但未定义

---

## 四、Footer 完整性标准

Footer 必须包含以下全部链接，缺任何一项=残缺：
1. 首页 (`../index.html`)
2. 联系我们 (`mailto:dexshuang@google.com`)
3. 隐私政策 (`../privacy/`)
4. 服务条款 (`../terms/`)
5. 关于我们 (`../about/`)
6. GitHub (`https://github.com/webtools-cn/tools-site`)
7. EN语言切换 (`../en/<tool-slug>/`)
8. 问题反馈邮箱行 (`问题反馈: dexshuang@google.com`)

**结构要求：** footer 必须位于 `</main>` 之后、`</body>` 之前；内容段（什么是/使用场景/FAQ）必须在 `</main>` 内，绝不能在 footer 内/后。

---

## 五、内容深度标准（AdSense 合规）

- **每页纯文字量 ≥ 800 字符**（去 HTML 标签、去 script/style 后计算）
- **内容段必须差异化**：不能全站雷同的模板句（"该工具完全在本地运行..."出现在500+页=违规）
- **h3 骨架差异化**：不能几百页共用同一标题（"计算原理"/"输入参数说明"）
- **FAQ 必须真实**：与工具功能相关的真实问题，不能占位
- **EN 内容必须人写英文**：不能机翻、不能中文残留

### 单薄工具判定（需增强）
- 纯文字量 < 800 字符 → 补"什么是/使用场景/工具特点/FAQ"差异化内容
- ≤3 输入 + ≤2 按钮 + 无 canvas + 无导出 → 功能增强（示例填充/复制/图表/历史）

---

## 六、相关推荐相关性规则

related-tools 区块的链接必须与当前工具**功能相关**（同类别），不能随机占位。
- ✅ 厨房单位换算器 → 体积换算器、重量换算器、温度换算器
- ❌ 厨房单位换算器 → 年龄计算器、BMI计算器

---

## 七、GA/AdSense 标准

- **GA ID**：`G-QVBQNJ3L5E`（新版正确 ID）
- **AdSense ID**：`ca-pub-5527959372219623`（新版正确 ID）
- ❌ 旧 ID：`G-9W1157EBQV` / `ca-pub-5998441792679372`（已废弃，全站大量页面还挂着，逐步替换）
- 每页必须有 GA 代码（gtag.js + config）

---

## 八、SEO 标准

- **meta description**：50-160 字符，差异化（不能全站雷同），`name="description"` 与 `content=` 顺序不限
- **canonical**：每页有正确的 canonical URL
- **hreflang**：中英互指 + x-default
- **JSON-LD**：SoftwareApplication + HowTo + BreadcrumbList（合法，不含假评分）
- **重定向页**（"已合并/已迁移"）必须加 `noindex, follow`
- **robots**：`index, follow`（正常页）
- **EN 首页链接**：`tools-data-en.json` 中每条 slug 必须带 `/en/` 前缀，且对应 `en/<slug>/index.html` 存在，不能有 undefined/中文残留/dict 格式错位

---

## 九、浏览器实测标准（每批修复后必做）

打开页面，3 秒检查：
1. **主题**：深色？输入框/输出框也是深色？
2. **功能**：填值 → 点主按钮 → 有输出？Console 无报错？
3. **语言**：CN 全中文？EN 全英文？
4. **相关推荐**：相关吗？
5. **Footer**：8 项齐全？

---

## 十、修复流程铁律

1. **批量脚本改完必须全站验证**：JS 语法 + 结构平衡 + 语言 + 字数，数字不归零不算完成
2. **修复必须全站扫同类归零**：不能只修报出的那页
3. **每个 commit 立即 push**：不攒批（本地领先 origin = 线上验证全误判）
4. **EN 修复用 CN 版当源时要小心**：CN 版可能被模板化丢失真实功能，先检查 EN 独有函数
5. **read_file 陷阱**：批量改文件用 Python `open().read()+write()`，不用 read_file 写回（行号前缀会损坏文件）
6. **JS 检查必须一次 node 调用**：收集全部代码块 → `new Function` 逐个编译，不能逐页 spawn node
7. **git 恢复优先**：JS 崩溃页先试 git 历史恢复（块级，按内容相似度匹配），成功率 ~90%
8. **subagent 只用于 ≤10 页 LLM 推理任务**：文件批量处理用脚本直接干

---

## 十一、当前已知问题（扫描基线 2026-08-11）

| 问题 | 数量 | 位置 |
|:-----|:----:|:-----|
| JS 语法失败 | 141 页 | `/tmp/js_fails_0811.json` |
| 未提交修改 | 14 个 EN 页 | git status |
| GA 旧 ID | 全站大部分 | 逐步替换 |

---

## 十二、验收标准

1. 全站 JS 语法失败 = 0
2. 全站结构平衡（div/section/main/footer）
3. EN 页面中文残留 = 0
4. 每页纯文字 ≥ 800 字符
5. 修复的页面浏览器实测通过
6. 质量门（quality_gate.py）9 项检测通过
