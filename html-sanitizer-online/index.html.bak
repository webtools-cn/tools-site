<!DOCTYPE html>
<html lang="zh-CN">
<head>
<!-- Google tag (gtag.js) -->
<script async src="https://www.googletagmanager.com/gtag/js?id=G-9W1157EBQV"></script>
<script>window.dataLayer = window.dataLayer || []; function gtag(){dataLayer.push(arguments);} gtag('js', new Date()); gtag('config', 'G-9W1157EBQV');</script>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<meta name="description" content="免费在线HTML消毒器，安全过滤HTML中的XSS攻击代码、危险标签和属性。支持白名单模式、标签过滤、属性清理、配置导出。纯前端本地处理，数据不上传服务器。">
<title>HTML消毒器 - 在线HTML Sanitizer · XSS过滤 · 安全清理 · 白名单模式</title>
<link rel="canonical" href="https://free-toolbase.com/html-sanitizer-online/">
<meta property="og:title" content="HTML消毒器 - 在线HTML Sanitizer · XSS过滤 · 安全清理 · 白名单模式">
<meta property="og:description" content="免费在线HTML消毒器，安全过滤HTML中的XSS攻击代码、危险标签和属性。支持白名单模式、标签过滤、属性清理、配置导出。纯前端本地处理，数据不上传服务器。">
<meta property="og:url" content="https://free-toolbase.com/html-sanitizer-online/">
<meta property="og:type" content="website">
<meta property="og:site_name" content="在线小工具矩阵">
<script type="application/ld+json">{"@context": "https://schema.org", "@type": "SoftwareApplication", "name": "HTML消毒器", "description": "免费在线HTML消毒器，安全过滤HTML中的XSS攻击代码、危险标签和属性。支持白名单模式、标签过滤、属性清理、配置导出。纯前端本地处理，数据不上传服务器。", "applicationCategory": "UtilitiesApplication", "operatingSystem": "Web", "offers": {"@type": "Offer", "price": "0", "priceCurrency": "CNY"}, "publisher": {"@type": "Organization", "name": "Online Tools", "email": "dexshuang@google.com"}}</script>
<script type="application/ld+json">{
  "@context": "https://schema.org",
  "@type": "FAQPage",
  "name": "什么是HTML消毒？",
  "mainEntity": [
    {
      "@type": "Question",
      "name": "什么是HTML消毒？",
      "acceptedAnswer": {
        "@type": "Answer",
        "text": "HTML消毒（HTML Sanitization）是过滤HTML代码中潜在危险内容的过程，包括移除script标签、事件处理属性（onclick等）、javascript:协议链接等XSS攻击向量，同时保留安全的HTML标签和属性。"
      }
    },
    {
      "@type": "Question",
      "name": "为什么需要HTML消毒？",
      "acceptedAnswer": {
        "@type": "Answer",
        "text": "当用户提交的HTML内容需要显示在页面上时（如评论、富文本编辑器），未消毒的HTML可能包含恶意脚本，导致XSS攻击。HTML消毒可以防止脚本注入，保护用户数据安全。"
      }
    },
    {
      "@type": "Question",
      "name": "白名单模式和黑名单模式有什么区别？",
      "acceptedAnswer": {
        "@type": "Answer",
        "text": "白名单模式只允许指定的安全标签和属性通过，其他全部移除，安全性更高。黑名单模式只移除指定的危险标签和属性，其他全部保留，灵活性更高但安全性较低。推荐使用白名单模式。"
      }
    },
    {
      "@type": "Question",
      "name": "哪些HTML标签和属性是危险的？",
      "acceptedAnswer": {
        "@type": "Answer",
        "text": "危险标签包括：script、iframe、object、embed、applet、form、input、meta、link、base。危险属性包括：所有on*事件属性（onclick、onerror等）、javascript:协议、data:协议、style中的expression()和url()。"
      }
    },
    {
      "@type": "Question",
      "name": "消毒后的HTML完全安全吗？",
      "acceptedAnswer": {
        "@type": "Answer",
        "text": "白名单模式消毒后的HTML在大多数场景下是安全的，但没有100%的安全保证。建议配合CSP（内容安全策略）使用，并定期更新白名单规则以应对新的攻击手法。"
      }
    }
  ]
}</script>
<style>
*{box-sizing:border-box;margin:0;padding:0}
body{background:#0f172a;color:#e2e8f0;font-family:-apple-system,BlinkMacSystemFont,"Segoe UI",Roboto,Helvetica,Arial,"PingFang SC","Microsoft YaHei",sans-serif;line-height:1.6;min-height:100vh}
a{color:#06b6d4;text-decoration:none}
.container{max-width:1100px;margin:0 auto;padding:24px 16px}
.header{display:flex;justify-content:space-between;align-items:center;margin-bottom:24px}
.header h1{font-size:1.6rem;color:#f1f5f9}
.lang-switch{display:flex;gap:4px;background:#1e293b;border-radius:8px;padding:4px;border:1px solid rgba(148,163,184,.1)}
.lang-switch a{padding:6px 12px;border-radius:5px;font-size:.85rem;color:#94a3b8}
.lang-switch a.active{background:rgba(6,182,212,.2);color:#22d3ee}
.nav-back{color:#64748b;font-size:.85rem;margin-bottom:16px}
.nav-back a{color:#64748b}
.tool-section{background:#1e293b;border-radius:12px;padding:20px;margin-bottom:16px;border:1px solid rgba(148,163,184,.1)}
.tool-section h2{font-size:1.1rem;color:#f1f5f9;margin-bottom:12px}
.btn-row{display:flex;gap:8px;flex-wrap:wrap;margin-top:12px}
.btn{padding:8px 20px;border:none;border-radius:6px;font-size:.85rem;cursor:pointer;transition:all .2s}
.btn-primary{background:rgba(6,182,212,.2);color:#22d3ee;border:1px solid rgba(6,182,212,.3)}
.btn-primary:hover{background:rgba(6,182,212,.3)}
.btn-secondary{background:rgba(148,163,184,.1);color:#94a3b8;border:1px solid rgba(148,163,184,.2)}
.btn-secondary:hover{background:rgba(148,163,184,.2)}
.btn-success{background:rgba(34,197,94,.15);color:#22c55e;border:1px solid rgba(34,197,94,.25)}
.btn-success:hover{background:rgba(34,197,94,.25)}
.btn-danger{background:rgba(239,68,68,.15);color:#f87171;border:1px solid rgba(239,68,68,.2)}
.btn-danger:hover{background:rgba(239,68,68,.25)}
textarea{width:100%;background:#0f172a;border:1px solid rgba(148,163,184,.2);border-radius:8px;padding:12px;color:#e2e8f0;font-family:monospace;font-size:.85rem;min-height:150px;resize:vertical;outline:none}
textarea:focus{border-color:rgba(6,182,212,.5)}
.result-box{background:#0f172a;border:1px solid rgba(148,163,184,.2);border-radius:8px;padding:12px;font-family:monospace;font-size:.85rem;min-height:40px;word-break:break-all;max-height:300px;overflow-y:auto;white-space:pre-wrap;margin-top:12px}
.info-section{background:#1e293b;border-radius:12px;padding:20px;margin-bottom:16px;border:1px solid rgba(148,163,184,.1)}
.info-section h2{font-size:1.1rem;color:#f1f5f9;margin-bottom:12px}
.info-section h3{font-size:.95rem;color:#e2e8f0;margin:16px 0 8px}
.info-section p{color:#94a3b8;font-size:.9rem;margin-bottom:8px}
.info-section ul,.info-section ol{color:#94a3b8;font-size:.9rem;margin:8px 0 8px 20px}
.info-section li{margin-bottom:4px}
.faq-item{margin-bottom:16px}
.faq-item h3{font-size:.95rem;color:#e2e8f0;margin-bottom:6px}
.faq-item p{color:#94a3b8;font-size:.9rem}
.footer{border-top:1px solid rgba(148,163,184,.1);padding:24px 0;margin-top:32px;text-align:center;color:#64748b;font-size:.85rem}
.footer a{color:#64748b;margin:0 8px}
.footer a:hover{color:#94a3b8}
.toast{position:fixed;bottom:20px;left:50%;transform:translateX(-50%);background:#1e293b;color:#22d3ee;padding:10px 24px;border-radius:8px;border:1px solid rgba(6,182,212,.3);font-size:.85rem;z-index:999;opacity:0;transition:opacity .3s}
.toast.show{opacity:1}
.options{display:flex;gap:12px;align-items:center;margin:8px 0;flex-wrap:wrap}
.options label{display:flex;align-items:center;gap:6px;font-size:.85rem;color:#94a3b8;cursor:pointer}
input[type=checkbox]{accent-color:#06b6d4}
select{background:#0f172a;border:1px solid rgba(148,163,184,.2);border-radius:6px;color:#e2e8f0;padding:6px 10px;font-size:.85rem;outline:none}
.tag-list{display:flex;flex-wrap:wrap;gap:4px;margin-top:8px}
.tag{display:inline-block;background:rgba(6,182,212,.1);color:#22d3ee;font-size:.75rem;padding:2px 8px;border-radius:4px;border:1px solid rgba(6,182,212,.2)}
.removed-item{color:#f87171;font-size:.8rem;padding:2px 0}
.stats{display:flex;gap:16px;margin-top:8px;flex-wrap:wrap}
.stat{background:rgba(148,163,184,.08);border-radius:8px;padding:8px 12px;text-align:center}
.stat .num{font-size:1.2rem;color:#22d3ee;font-weight:700}
.stat .lbl{font-size:.7rem;color:#64748b}
.grid-2{display:grid;grid-template-columns:1fr 1fr;gap:16px}
@media(max-width:768px){.grid-2{grid-template-columns:1fr}}
.ad-slot{margin:16px auto;text-align:center;max-width:960px;min-height:90px}
.ad-slot:empty{display:none}
.ad-slot ins{display:block}
.ad-slot.ad-sidebar{min-height:250px;max-width:300px}
</style>
<script async src="https://pagead2.googlesyndication.com/pagead/js/adsbygoogle.js?client=ca-pub-5998441792679372" crossorigin="anonymous"></script>
<link rel="alternate" hreflang="zh" href="https://free-toolbase.com/html-sanitizer-online/">
<link rel="alternate" hreflang="en" href="https://free-toolbase.com/en/html-sanitizer-online/">
<link rel="alternate" hreflang="x-default" href="https://free-toolbase.com/en/html-sanitizer-online/">
</head>
<body>
<div class="ad-slot" id="ad-top"><ins class="adsbygoogle" style="display:block" data-ad-client="ca-pub-5998441792679372" data-ad-slot="XXXXXXX" data-ad-format="horizontal" data-full-width-responsive="true"></ins></div>
<div class="container">
<div class="header">
<h1>🛡️ HTML消毒器</h1>
<div class="lang-switch"><a href="/">🏠 首页</a><a href="/html-sanitizer-online/" class="active">中文</a><a href="/en/html-sanitizer-online/">EN</a></div>
</div>
<div class="nav-back"><a href="/">← 返回首页</a> › <a href="/tools/dev/">开发工具</a> › HTML消毒器</div>
<p style="color:#94a3b8;font-size:.9rem;margin-bottom:8px">安全过滤XSS攻击代码、危险标签和属性，白名单模式保障安全</p>
<span style="display:inline-block;background:rgba(6,182,212,.15);color:#22d3ee;font-size:.75rem;padding:2px 8px;border-radius:4px;border:1px solid rgba(6,182,212,.3)">零依赖·可离线使用</span>

<div class="tool-section">
<h2>📝 输入HTML</h2>
<textarea id="input-html" placeholder="粘贴需要消毒的HTML代码..." oninput="sanitize()"></textarea>
<div class="btn-row">
<button class="btn btn-primary" onclick="sanitize()">🛡️ 消毒</button>
<button class="btn btn-secondary" onclick="loadExample()">📋 加载示例</button>
<button class="btn btn-danger" onclick="clearAll()">🗑️ 清空</button>
</div>
</div>

<div class="grid-2">
<div>
<div class="tool-section">
<h2>⚙️ 消毒模式</h2>
<div class="options">
<label><input type="radio" name="mode" value="whitelist" checked onchange="sanitize()"> 白名单模式（推荐）</label>
<label><input type="radio" name="mode" value="blacklist" onchange="sanitize()"> 黑名单模式</label>
</div>
<div style="margin-top:12px">
<h3 style="font-size:.9rem;color:#e2e8f0;margin-bottom:8px">白名单允许的标签：</h3>
<div class="tag-list" id="allowed-tags"></div>
</div>
<div style="margin-top:12px">
<h3 style="font-size:.9rem;color:#e2e8f0;margin-bottom:8px">允许的属性：</h3>
<div class="tag-list" id="allowed-attrs"></div>
</div>
</div>
</div>
<div>
<div class="tool-section">
<h2>📊 消毒统计</h2>
<div class="stats" id="stats">
<div class="stat"><div class="num" id="stat-tags">0</div><div class="lbl">移除标签</div></div>
<div class="stat"><div class="num" id="stat-attrs">0</div><div class="lbl">移除属性</div></div>
<div class="stat"><div class="num" id="stat-comments">0</div><div class="lbl">移除注释</div></div>
</div>
<div style="margin-top:12px">
<h3 style="font-size:.9rem;color:#f87171;margin-bottom:8px">已移除内容：</h3>
<div id="removed-list" style="max-height:150px;overflow-y:auto"></div>
</div>
</div>
</div>
</div>

<div class="tool-section">
<h2>✅ 消毒结果</h2>
<div class="btn-row" style="margin-top:0;margin-bottom:8px">
<button class="btn btn-success" onclick="copyResult()">📋 复制结果</button>
<button class="btn btn-secondary" onclick="downloadResult()">💾 下载HTML</button>
<button class="btn btn-secondary" onclick="previewResult()">👁️ 预览</button>
</div>
<div class="result-box" id="output-html"></div>
</div>

<div class="ad-slot" style="margin-top:24px"><ins class="adsbygoogle" style="display:block" data-ad-client="ca-pub-5998441792679372" data-ad-slot="XXXXXXX" data-ad-format="horizontal" data-full-width-responsive="true"></ins></div>

<div class="info-section">
<h2>📖 HTML消毒器能做什么？</h2>
<p>HTML消毒器是一个在线安全工具，用于过滤HTML代码中的潜在危险内容。它可以移除XSS攻击向量（如script标签、事件处理属性、javascript:协议），同时保留安全的HTML标签和属性，确保用户提交的HTML内容可以安全显示。</p>
<h2>核心功能</h2>
<ul>
<li><strong>白名单模式</strong>：只允许指定的安全标签和属性通过，安全性最高</li>
<li><strong>黑名单模式</strong>：只移除已知的危险标签和属性，保留其他内容</li>
<li><strong>XSS过滤</strong>：移除script、事件属性、javascript:协议等攻击向量</li>
<li><strong>实时统计</strong>：显示移除的标签、属性、注释数量和详情</li>
<li><strong>安全预览</strong>：在沙箱iframe中预览消毒后的HTML效果</li>
</ul>
<h2>使用教程</h2>
<ol>
<li>在输入框粘贴需要消毒的HTML代码</li>
<li>选择消毒模式（推荐白名单模式）</li>
<li>工具自动执行消毒，显示结果和统计</li>
<li>查看移除内容列表确认过滤是否正确</li>
<li>复制消毒结果或下载为HTML文件</li>
</ol>
<h2>应用场景</h2>
<h3>场景1：Web开发者</h3>
<p>
<p>在显示用户提交的HTML内容前进行消毒，防止XSS攻击，保护网站和用户安全。</p>
<h3>场景2：内容管理系统</h3>
<p>对富文本编辑器输出的HTML进行安全过滤，确保只保留安全的格式标签。</p>
<h3>场景3：安全审计</h3>
<p>分析HTML代码中的潜在安全风险，识别可能被利用的攻击向量。</p>
<h2>扩展知识</h2>
<p>XSS（跨站脚本攻击）是最常见的Web安全漏洞之一。攻击者通过注入恶意脚本到网页中，可以窃取用户Cookie、会话令牌，甚至控制用户浏览器。HTML消毒是防御XSS的重要手段，通过移除危险的HTML元素和属性，阻断脚本执行路径。OWASP推荐使用白名单方式进行HTML消毒，因为黑名单方式容易遗漏新的攻击手法。</p>
</div>

<div class="info-section">
<h2>📖 常见问题</h2>
<div class="faq-item"><h3>什么是HTML消毒？</h3><p>HTML消毒是过滤HTML代码中潜在危险内容的过程，包括移除script标签、事件处理属性、javascript:协议链接等XSS攻击向量，同时保留安全的HTML标签和属性。</p></div>
<div class="faq-item"><h3>为什么需要HTML消毒？</h3><p>当用户提交的HTML内容需要显示在页面上时，未消毒的HTML可能包含恶意脚本，导致XSS攻击。HTML消毒可以防止脚本注入，保护用户数据安全。</p></div>
<div class="faq-item"><h3>白名单模式和黑名单模式有什么区别？</h3><p>白名单模式只允许指定的安全标签和属性通过，其他全部移除，安全性更高。黑名单模式只移除指定的危险标签和属性，其他全部保留，灵活性更高但安全性较低。推荐使用白名单模式。</p></div>
<div class="faq-item"><h3>哪些HTML标签和属性是危险的？</h3><p>危险标签包括：script、iframe、object、embed、applet、form、input、meta、link、base。危险属性包括：所有on*事件属性、javascript:协议、data:协议、style中的expression()。</p></div>
<div class="faq-item"><h3>消毒后的HTML完全安全吗？</h3><p>白名单模式消毒后的HTML在大多数场景下是安全的，但没有100%的安全保证。建议配合CSP（内容安全策略）使用，并定期更新白名单规则。</p></div>
</div>

<div class="ad-slot" style="margin-top:24px"><ins class="adsbygoogle" style="display:block" data-ad-client="ca-pub-5998441792679372" data-ad-slot="XXXXXXX" data-ad-format="horizontal" data-full-width-responsive="true"></ins></div>

<div id="feedback-widget" style="position:fixed;bottom:20px;right:20px;z-index:1000">
<button id="feedback-toggle" onclick="toggleFeedback()" style="width:48px;height:48px;border-radius:50%;background:rgba(6,182,212,.2);border:2px solid rgba(6,182,212,.5);color:#22d3ee;font-size:1.2rem;cursor:pointer;box-shadow:0 4px 12px rgba(0,0,0,.3);transition:all .2s">💬</button>
<div id="feedback-panel" style="display:none;position:absolute;bottom:56px;right:0;width:320px;background:#1e293b;border:1px solid rgba(6,182,212,.3);border-radius:12px;padding:16px;box-shadow:0 8px 24px rgba(0,0,0,.4)">
<div style="display:flex;justify-content:space-between;align-items:center;margin-bottom:12px"><span style="color:#f1f5f9;font-size:.95rem;font-weight:600">📝 反馈建议</span><button onclick="toggleFeedback()" style="background:none;border:none;color:#64748b;font-size:1.2rem;cursor:pointer">✕</button></div>
<select id="feedback-type" style="width:100%;background:#0f172a;border:1px solid rgba(148,163,184,.2);border-radius:6px;padding:8px 10px;color:#e2e8f0;font-size:.85rem;margin-bottom:8px;outline:none"><option value="feedback">💡 功能建议</option><option value="bug">🐛 问题反馈</option><option value="other">💬 其他</option></select>
<textarea id="feedback-text" placeholder="请描述您的建议或遇到的问题..." style="width:100%;height:100px;background:#0f172a;border:1px solid rgba(148,163,184,.2);border-radius:6px;padding:10px;color:#e2e8f0;font-size:.85rem;resize:vertical;outline:none;font-family:inherit" maxlength="1000"></textarea>
<div style="display:flex;justify-content:space-between;align-items:center;margin-top:8px"><span id="feedback-count" style="color:#475569;font-size:.75rem">0/1000</span><button onclick="submitFeedback()" style="background:rgba(6,182,212,.2);color:#22d3ee;border:1px solid rgba(6,182,212,.3);border-radius:6px;padding:6px 16px;font-size:.85rem;cursor:pointer">提交</button></div>
</div>
</div>

<div class="footer container">
<div style="margin-bottom:12px">
<a href="/">首页</a>
<a href="/">全部工具</a>
<a href="mailto:dexshuang@google.com">联系我们</a>
<a href="/privacy/">隐私政策</a>
<a href="/terms/">服务条款</a>
<a href="/about/">关于我们</a>
<a href="/en/html-sanitizer-online/">EN</a>
</div>
<p>HTML消毒器 · 纯前端本地处理 · 数据绝不上传服务器</p>
<p style="margin-top:8px;color:#475569;font-size:.8rem">问题反馈: dexshuang@google.com</p>
</div>
</div>

<script>
const ALLOWED_TAGS = ['p','br','b','i','u','em','strong','a','ul','ol','li','h1','h2','h3','h4','h5','h6','blockquote','code','pre','hr','table','thead','tbody','tr','th','td','img','span','div','dl','dt','dd','sup','sub','abbr','mark','del','ins','figure','figcaption','details','summary'];
const ALLOWED_ATTRS = ['href','title','alt','src','width','height','class','id','target','rel','colspan','colspan','rowspan','rel','datetime','cite'];
const DANGEROUS_TAGS = ['script','style','iframe','object','embed','applet','form','input','button','select','textarea','meta','link','base','noscript','template','svg','math'];
const DANGEROUS_ATTR_PATTERNS = [/^on/i, /^data-/i];

let removedTags = 0, removedAttrs = 0, removedComments = 0;
let removedItems = [];

function initTagDisplay() {
  document.getElementById('allowed-tags').innerHTML = ALLOWED_TAGS.map(t => `<span class="tag">${t}</span>`).join('');
  document.getElementById('allowed-attrs').innerHTML = ALLOWED_ATTRS.map(a => `<span class="tag">${a}</span>`).join('');
}

function sanitize() {
  const input = document.getElementById('input-html').value;
  if (!input.trim()) { document.getElementById('output-html').textContent = ''; return; }
  
  removedTags = 0; removedAttrs = 0; removedComments = 0;
  removedItems = [];
  
  const mode = document.querySelector('input[name="mode"]:checked').value;
  let result = input;
  
  // Remove HTML comments
  result = result.replace(/<!--[\s\S]*?-->/g, (m) => { removedComments++; removedItems.push({type:'comment', val:m.substring(0,50)}); return ''; });
  
  // Parse and process tags
  result = result.replace(/<\s*\/?\s*([a-zA-Z][a-zA-Z0-9]*)\s*([^>]*)\/?\s*>/g, (match, tag, attrs) => {
    const tagLower = tag.toLowerCase();
    
    if (mode === 'whitelist') {
      if (!ALLOWED_TAGS.includes(tagLower)) {
        removedTags++;
        removedItems.push({type:'tag', val:tagLower});
        return '';
      }
    } else {
      if (DANGEROUS_TAGS.includes(tagLower)) {
        removedTags++;
        removedItems.push({type:'tag', val:tagLower});
        return '';
      }
    }
    
    // Process attributes
    let cleanAttrs = attrs.replace(/([a-zA-Z][a-zA-Z0-9-]*)\s*=\s*(?:"[^"]*"|'[^']*'|[^\s>]+)/g, (attrMatch, attrName) => {
      const attrLower = attrName.toLowerCase();
      
      if (mode === 'whitelist') {
        if (!ALLOWED_ATTRS.includes(attrLower)) {
          removedAttrs++;
          removedItems.push({type:'attr', val:attrLower});
          return '';
        }
      } else {
        if (DANGEROUS_ATTR_PATTERNS.some(p => p.test(attrLower))) {
          removedAttrs++;
          removedItems.push({type:'attr', val:attrLower});
          return '';
        }
      }
      
      // Check for javascript: protocol in href/src
      const valMatch = attrMatch.match(/=\s*(?:"([^"]*)"|'([^']*)'|(\S+))/);
      if (valMatch) {
        const val = (valMatch[1] || valMatch[2] || valMatch[3] || '').toLowerCase();
        if (val.includes('javascript:') || val.includes('data:text/html')) {
          removedAttrs++;
          removedItems.push({type:'attr', val:attrLower+' (dangerous protocol)'});
          return '';
        }
      }
      return attrMatch;
    });
    
    cleanAttrs = cleanAttrs.replace(/\s+/g, ' ').trim();
    const isClosing = match.includes('</');
    const isSelfClosing = match.includes('/>');
    if (isClosing) return `</${tagLower}>`;
    if (isSelfClosing) return cleanAttrs ? `<${tagLower} ${cleanAttrs}/>` : `<${tagLower}/>`;
    return cleanAttrs ? `<${tagLower} ${cleanAttrs}>` : `<${tagLower}>`;
  });
  
  // Clean up empty lines
  result = result.replace(/\n\s*\n\s*\n/g, '\n\n').trim();
  
  document.getElementById('output-html').textContent = result;
  document.getElementById('stat-tags').textContent = removedTags;
  document.getElementById('stat-attrs').textContent = removedAttrs;
  document.getElementById('stat-comments').textContent = removedComments;
  
  const list = document.getElementById('removed-list');
  list.innerHTML = removedItems.length === 0 
    ? '<p style="color:#64748b;font-size:.85rem">未发现危险内容</p>'
    : removedItems.map(i => `<div class="removed-item">❌ [${i.type}] ${i.val}</div>`).join('');
}

function loadExample() {
  document.getElementById('input-html').value = `<div class="content">
  <h1 onclick="alert('XSS')">标题</h1>
  <p>这是一段安全的文字</p>
  <script>document.cookie<\/script>
  <a href="javascript:alert(1)">危险链接</a>
  <a href="https://example.com" title="安全链接">安全链接</a>
  <img src="x" onerror="alert(1)" alt="图片">
  <iframe src="https://evil.com"></iframe>
  <!-- 隐藏注释 -->
  <b>加粗文字</b>
  <em style="background:url(javascript:alert(1))">强调</em>
</div>`;
  sanitize();
}

function clearAll() {
  document.getElementById('input-html').value = '';
  document.getElementById('output-html').textContent = '';
  document.getElementById('stat-tags').textContent = '0';
  document.getElementById('stat-attrs').textContent = '0';
  document.getElementById('stat-comments').textContent = '0';
  document.getElementById('removed-list').innerHTML = '';
}

function copyResult() {
  const text = document.getElementById('output-html').textContent;
  if (!text) { showToast('请先执行消毒'); return; }
  navigator.clipboard.writeText(text).then(() => showToast('✅ 已复制到剪贴板'));
}

function downloadResult() {
  const text = document.getElementById('output-html').textContent;
  if (!text) { showToast('请先执行消毒'); return; }
  const blob = new Blob([text], {type:'text/html'});
  const a = document.createElement('a');
  a.href = URL.createObjectURL(blob);
  a.download = 'sanitized.html';
  a.click();
}

function previewResult() {
  const text = document.getElementById('output-html').textContent;
  if (!text) { showToast('请先执行消毒'); return; }
  const w = window.open('', '_blank', 'width=600,height=400');
  w.document.write(`<!DOCTYPE html><html><head><style>body{font-family:sans-serif;padding:20px;background:#1e293b;color:#e2e8f0}</style></head><body>${text}<div class="footer" style="text-align:center;color:#64748b;font-size:.85rem;margin-top:32px;padding-top:20px;border-top:1px solid rgba(148,163,184,.1)">
<p><a href="/" style="color:#64748b">首页</a> | <a href="/privacy-policy/" style="color:#64748b">隐私政策</a> | <a href="/terms-of-service/" style="color:#64748b">服务条款</a></p>
<p style="margin-top:8px">© 2026 Free ToolBase - 纯前端本地运行·不上传服务器</p>
</div>
</body></html>`);
  w.document.close();
}

function toggleFeedback(){const p=document.getElementById('feedback-panel');p.style.display=p.style.display==='none'?'block':'none';}
document.getElementById('feedback-text')?.addEventListener('input',function(){document.getElementById('feedback-count').textContent=this.value.length+'/1000';});
function submitFeedback(){
const type=document.getElementById('feedback-type').value;
const text=document.getElementById('feedback-text').value.trim();
if(!text){return;}
const title=encodeURIComponent(type+' - '+window.location.pathname);
const body=encodeURIComponent('**Page**: '+window.location.href+'\\n**Type**: '+type+'\\n\\n'+text);
window.open('https://github.com/webtools-cn/tools-site/issues/new?title='+title+'&body='+body,'_blank');
}
function showToast(msg){const t=document.getElementById('toast')||(()=>{const d=document.createElement('div');d.id='toast';d.className='toast';document.body.appendChild(d);return d;})();t.textContent=msg;t.classList.add('show');setTimeout(()=>t.classList.remove('show'),2000);}

window.addEventListener('load', function() { initTagDisplay(); loadExample(); });
document.addEventListener('keydown', function(e) {
  if (e.ctrlKey && e.key === 'Enter') { sanitize(); e.preventDefault(); }
  if (e.ctrlKey && e.shiftKey && e.key === 'C') { copyResult(); e.preventDefault(); }
});
</script>
<link rel="stylesheet" href="https://free-toolbase.com/related-tools.css">
<div id="related-tools-section" class="related-tools-section"><div class="related-tools-loading">Loading related tools...</div></div>
<script src="https://free-toolbase.com/related-tools.js"></script>
<div class="footer" style="text-align:center;color:#64748b;font-size:.85rem;margin-top:32px;padding-top:20px;border-top:1px solid rgba(148,163,184,.1)">
<p><a href="/" style="color:#64748b">首页</a> | <a href="/privacy-policy/" style="color:#64748b">隐私政策</a> | <a href="/terms-of-service/" style="color:#64748b">服务条款</a></p>
<p style="margin-top:8px">© 2026 Free ToolBase - 纯前端本地运行·不上传服务器</p>
</div>
</body>
</html>